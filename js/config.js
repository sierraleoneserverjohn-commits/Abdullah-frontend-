const AppConfig = {
  appName: "Abdullah AI",
  version: "1.0.0",
  userTarget: "Sana",
  apiEndpoint: "/api/chat", // Replace with custom backend endpoint when ready
  useLocalFallback: true,
  themeDefaults: {
    sound: true,
    voice: true,
    autoSpeak: true,
    animations: true
  }
};
